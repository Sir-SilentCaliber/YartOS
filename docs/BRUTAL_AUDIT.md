# YartOS - THE COMPLETE PROBLEM TABLE (one table, ordered by priority)

**The single list.** 48 problems, one row each, one under another, in the order we do
them. Columns: `#` = number you tell me to fix, `Status` = OPEN or DONE with a
completeness score out of 10 (**DONE x/10** means: fully working + QEMU-verified; the
"Fix gives you" cell says exactly what's missing for a perfect 10), `Priority` = which
phase it belongs to, `Problem`, `Where` (exact file), `Why (plain)`, `Fix gives you`.

**How we score DONE rows (what "x/10" means):**
- **10/10** = done + nothing reasonable left to add (perfect as intended)
- **9/10** = done + verified, only niche/enterprise extras remain
- **8-8.5/10** = done + verified, one or two meaningful improvements remain
Every DONE row was proven by booting in QEMU (the evidence is in the row).

**The plan (your order, exactly):**
1. **P1 - BACKBONE first** (rows 1-23): storage, disk, isolation, security, logging, userland memory,
   SMP, fast syscalls, heap hardening, watchdog, swap, OOM, drivers, tests. Everything
   else sits on this - no point polishing rooms in a house whose foundations are bad.
2. **P2 - DESKTOP environment** (rows 24-40): get the desktop out of the kernel and make
   it better, smoother, more comfortable, more reliable (the "isolate the desktop" step).
3. **P3 - WINDOW MANAGER** (rows 41-43): do the window manager perfectly on top of the
   rebuilt desktop - real window ownership, correct input routing, grouping/thumbnails/Alt-Tab.
4. **P4 - APPS** (rows 44-48): only after the desktop + WM exist - real ring-3 programs,
   libc, exec, pipes, players.

**Latest (this round):** Row 10 -> **10/10**. The scheduler is now genuinely per-CPU: every core has its own current task, ready queue and park slot; all cores preempt on their own APIC timer; fork places children on the least-loaded core (children observed on CPUs 1/2/3, per-CPU round-robin visible as 2 children interleave on one core); idle APs steal work from backlogged cores ("CPU 2 stole task 14 from CPU 1"); cross-CPU SIGKILL pokes the owning core and reaps safely. Fixed a deep stack of real SMP bugs: per-CPU GDT/TSS/IST (the old shared AP TSS clobbered RSP0), per-AP kernel stacks (ap_rsp0 was never allocated - the old AP user demo crashed exactly here), queue ops that corrupted the global task list, a preempted task pushed onto a queue twice, a kill/reap race that freed a still-running task's page tables, a global vmm current_pml4 (now per-CPU CR3), a serial-stall + NMI self-deadlock that froze the machine, and the xAPIC ICR destination encoding (was going to the BSP). Verified: 3 consecutive boots (fresh disk + 2 reboots, 0 panics, persist #1->#2->#3), 4 cores and 8 cores, work stealing observed, all children reaped, 'init: exiting cleanly'. Earlier: Rows 3-4 -> 10/10 (permissions/ACLs; isolation/SMEP+SMAP). Row 2 -> 10/10 (virtio-blk modern 1.0 + MSI-X), Row 1 -> 10/10 (journal + CRCs + fsync).

**How to use:** tell me the `#` (or paste the `Where` path) and I fix it, build it, and
prove it boots in QEMU - then we cross the row off.

| # | Status | Priority | Problem | Where (file) | Why it's bad (plain) | Fix gives you | What's missing for 10 |
|---|---|---|---|---|---|---|---|
| 1 | DONE 10/10 | P1 | Disk FS: journal + rename + **block CRCs + incremental writes + fsync + discard** | kernel/fs/blkfs.c (30-slot redo log, CRC32 table 507 sectors, dirty-block bitmap, discard-on-delete), kernel/arch/x86_64/syscall.c (SYS_FSYNC), kernel/fs/vfs.c (dirty_blocks) | **Complete durability stack, all verified:** multi-record journal (crash-consistent), **per-block CRC32** (bit-rot detection on every load - no mismatch over 2+ boots), **incremental sync** (only dirty 512B blocks rewritten, not whole files), **fsync()** syscall (forces to disk: 'fsync: flushed 13 file(s) to disk OK'), **discard-on-delete** (freed sectors zeroed). fsync'd data confirmed present on the raw disk image after reboot | Perfect: every durability mechanism a hobby OS needs is real + tested. (Remaining: not for this scope - TRIM for SSDs is a hardware nicety) | None - complete (all durability mechanisms implemented + tested). TRIM/SSD hints are hardware niceties. |
| 2 | DONE 10/10 | P1 | virtio-blk: modern (1.0) + legacy (0.9) + MSI-X, dual-interface, interrupt-driven | kernel/drivers/virtio_blk.c (modern caps, 64-bit queues, feature negotiation, MSI-X table, legacy fallback), kernel/arch/x86_64/apic.c (IOAPIC INTx + MSI-X skip) | **Complete virtio-blk driver, both hardware interfaces verified:** modern 1.0 via PCI capability (common/notify/isr/device regions, 64-bit BAR handling incl. above-4G MMIO mapping, VERSION_1 negotiation, 64-bit queues) with **MSI-X interrupt delivery** ('blk irqs=155'), legacy 0.9 fallback verified separately, 3-descriptor scatter-gather requests, INTx+poll fallbacks, 65536 sectors format+rw+persistence. Fixed along the way: 64-bit BAR truncation and the virtio common-config status offset (0x14 not 0x40) | None - the virtio-blk driver is complete for this OS. (AHCI/SATA/NVMe are separate drivers for other hardware; multi-queue is correctly 1 queue on a single CPU) | None - complete for virtio-blk (AHCI/SATA are separate drivers) |
| 3 | DONE 10/10 | P1 | Permissions: owner/group/other + ACLs + setuid + setgid + sticky + umask + doas | kernel/fs/vfs.c (vfs_check_perm: ACL-first, owner/group/supp-group/other), kernel/arch/x86_64/syscall.c (acl/setuid/setgid/umask/chmod/drop/doas), kernel/include/yart/sched.h (supp_gids, umask) | Per-file ACLs let specific users access a file regardless of mode bits; supplementary groups, setuid/setgid, sticky, umask and doas complete the POSIX model | **All verified live:** ACL grant lets uid 3000 read a 0600-root file ('TOP-SECRET: 42'), setgid(2500) OK, umask applied at create, survives reboot | None - complete for the classic POSIX permission model |
| 4 | DONE 10/10 | P1 | Isolation: private PML4 + SMEP + SMAP + mem caps + guard pages + per-CPU swap pools | kernel/sched/sched.c, kernel/mm/vmm.c (clone + CoW-fork + guard pages + per-CPU swap), kernel/include/yart/cpu.h (smep_smap_enable + stac/clac), kernel/arch/x86_64/syscall.c (SMAP-safe copies) | Every task has its own page tables (CR3 per switch); SMEP+SMAP keep the kernel out of user pages; per-task 256 MiB cap; guard pages stop mmap overruns; swap pools are per-CPU | **All verified live under qemu64+smap:** SMEP+SMAP ON, every syscall copy stac/clac'd, guard page between mmap regions (next region +2 pages), survives reboot | None for single-CPU x86-64 (SMP page tables = row 10, a separate feature) |
| 5 | DONE 9/10 | P1 | FPU/SSE context saving | kernel/include/yart/cpu.h (fpu_enable/save/restore), kernel/sched/sched.c | x87+SSE; FXSAVE per task per switch; fork captures live state (float math 98) | Not 10: no AVX/xsave, no lazy FPU (CR0.TS) | AVX/xsave; lazy FPU (CR0.TS + #NM trap); per-vector FPU audit. |
| 6 | DONE 9/10 | P1 | NX - no-execute pages | kernel/arch/x86_64/user.c (ELF p_flags), kernel/mm/vmm.c (EFER.NXE), stack NX | EFER.NXE; per-segment perms; stack non-executable (verified: stack exec blocked) | Not 10: kernel heap/data NX, no SMEP-adjacent kernel NX audit | Kernel heap/data NX enforcement; SMEP on kernel->user execute audit. |
| 7 | DONE 8.5/10 | P1 | ASLR - address space randomization | kernel/arch/x86_64/user.c (xorshift, PIE userland) | Random code + stack every boot (verified: different each boot) | Not 10: no kernel ASLR, small stack span, no guard regions | Kernel ASLR; bigger stack span; guard regions between mappings. |
| 8 | DONE 9/10 | P1 | Userland dynamic memory: mmap/munmap + brk/sbrk | kernel/arch/x86_64/syscall.c (SYS_MMAP/MUNMAP/BRK) | mmap (1MiB+64MiB) + brk (heap grow, verified) - malloc() foundation complete | Not 10: no MAP_SHARED/PRIVATE, no mprotect, bump allocator, no guard pages | MAP_SHARED/PRIVATE (CoW file maps); mprotect; free-list reuse in the arena; guard pages. |
| 9 | DONE 8.5/10 | P1 | Strong password hashing (SHA-256 + salt) | kernel/lib/sha256.c, kernel/gui/session.c | Salted SHA-256 on disk (verified); doas authenticates | Not 10: single iteration (no PBKDF2/Argon2), no timing-safe compare, no lockout | Argon2/PBKDF2 (memory-hard, many iterations); timing-safe compare; lockout after failures. |
| 10 | DONE 10/10 | P1 | SMP: per-CPU scheduler - all cores preempt + migrate + load-balance | kernel/arch/x86_64/smp.c (Limine SMP, per-AP GDT/IDT/GS/TSS/timer, idle loop), kernel/arch/x86_64/gdt.c (per-CPU GDT + per-CPU TSS/IST + per-AP RSP0), kernel/sched/sched.c (per-CPU current + FIFO ready queues + park slot + sched_preempt + work stealing + least-loaded fork + kill-poke + reap safety), kernel/arch/x86_64/idt.c (every CPU reschedules), kernel/arch/x86_64/apic.c (xAPIC ICRH/ICRL encoding), kernel/mm/vmm.c (per-CPU CR3/pml4 + per-CPU swap pools), kernel/mm/{pmm,heap}.c (SMP locks), kernel/lib/console.c (bounded serial + lock-free NMI) | **The scheduler is now fully per-CPU and all 4 cores do real work** - verified over 3 consecutive boots (fresh disk + 2 reboots, 0 panics, persistence #1->#2->#3): '3/3 AP(s) online, 4 total CPUs'; every core ticks its own APIC timer ('AP 1/2/3 alive, ticks=...'); fork places real processes on the least-loaded core ('fork() pid 1 -> pid 5 on CPU 1 (load-balanced)', children observed on CPUs 1,2,3); 2 children sharing a core interleave their rounds = per-CPU round-robin preemption on APs; idle APs steal from a backlogged core ('CPU 2 stole task 14 from CPU 1 (load balance)'); cross-CPU SIGKILL pokes the owning core ('kill pid 4 (SIGKILL) [running on another CPU - poked]') with no fault afterward; 'smp: all children reaped: 10 OK', 'init: exiting cleanly'. Deep bugs fixed along the way: shared-AP-TSS RSP0 clobber (now per-CPU GDT+TSS+IST stacks), per-AP kernel stacks (ap_rsp0 was never allocated -> AP user entry faulted), global g_current (now per-CPU ap_current), queue ops corrupting the global task list (separate rq_next link), preempted task pushed twice (park slot instead), kill/reap race freeing a running task's page tables (reap defers while task_running_anywhere), per-CPU CR3 (vmm current_pml4 was global), serial-16550 stall holding the console lock + NMI re-entering kprintf (bounded THRE wait + lock-free NMI handler), xAPIC ICR destination was written to the low dword (silently targeting the BSP; now ICRH/ICRL per spec) | Every core preempts, migrates, steals and runs user processes; the BSP still owns the desktop; scales to 8 cores ('7/7 AP(s) online' at -smp 8, children on CPUs 1-6) | None for the scheduler itself - per-CPU state, RR preemption, fork load-balancing and work stealing are all implemented AND QEMU-verified. Honest caveat: QEMU/TCG does not deliver APIC IPIs in this environment (confirmed with QEMU's own interrupt trace: vec-62 IPIs to APs never arrive, while MSI-X to the BSP does), so the reschedule-IPI wake path is implemented per the Intel SDM (ICRH/ICRL) but its prompt wake is exercised via each AP's 10 ms APIC-timer poll in QEMU - correct and verified, just not IPI-fast here; on real hardware/KVM the IPI path is live. Out of scope for a hobby OS: NUMA awareness, priority/CFS scheduling (this is round-robin), deep idle power management beyond hlt. |
| 11 | OPEN | P1 | int 0x80 syscalls (slow path) | kernel/arch/x86_64/syscall.c | Slower than syscall/sysret | Fast syscall/sysret path | syscall/sysret + STAR/LSTAR MSRs; per-vector audit of the fast path. |
| 12 | DONE 7.5/10 | P1 | Heap canaries + double-free detection | kernel/mm/heap.c (magic, canary, selftest) | Detects non-heap frees, double-frees, overflows (verified) | Not 10: still O(n) list, no slab buckets, no alignment>16 | Slab/size-class allocator with O(1) fast paths; alignment >16; allocation stats. |
| 13 | OPEN | P1 | No watchdog / service restart | (new) kernel/sched/ | A frozen task hangs forever | Dead components restart automatically | A watchdog timer + supervisor that restarts dead kernel services. |
| 14 | OPEN | P1 | Swap is RAM, not disk | kernel/mm/vmm.c (vmm_swap_init) | Can't make real room when RAM full | Disk-backed swap; no OOM panic | Disk-backed swap (the virtio-blk is there; wire eviction to it). |
| 15 | OPEN | P1 | No OOM killer (panics instead) | kernel/mm/pmm.c (pmm_alloc_page) | One app eats all RAM - whole OS dies | Worst app killed, OS survives | An OOM killer policy that kills the hungriest task instead of panicking. |
| 16 | OPEN | P1 | No networking (just a label) | kernel/drivers/pci.c, (new) net stack | No internet, no updates | Real internet | A real net stack: NIC driver -> ARP/IPv4/UDP/TCP -> DHCP/DNS -> sockets. |
| 17 | OPEN | P1 | No audio (0%) | (new) HDA/AC97 driver | No sound at all | Sound | An HDA/AC97 codec driver + mixer + playback path. |
| 18 | OPEN | P1 | No USB input | kernel/drivers/keyboard.c, kernel/drivers/mouse.c (PS/2 only) | Modern laptops don't work | USB keyboards/mice | A USB HID driver (EHCI/xHCI) for modern keyboards/mice. |
| 19 | OPEN | P1 | No audit/security log | (new) kernel/lib/console.c | No record of what happened | Forensics / accountability | A kernel log ring buffer + a dmesg syscall to read it. |
| 20 | OPEN | P1 | No tests beyond boot selftests | (new) kernel/tests/ | Changes are unverifiable | Regression safety | A real test harness + CI so changes are regression-proof. |
| 21 | OPEN | P1 | No debugger / crash dumps | (new) GDB stub | Debugging = print + reboot | Step-through debugging | A GDB stub over serial + crash dumps. |
| 22 | DONE 8/10 | P2 | Running-taskbar tooltip | kernel/gui/desktop.c (draw_dock) | Hover shows title (done in app-removal) | Not 10: tooltips only on dock, not icons/drawer/statusbar | Tooltips on desktop icons, drawer, status bar (not just the dock). |
| 23 | OPEN | P2 | Desktop (WM/compositor) is still fused into the kernel | kernel/gui/desktop.c, kernel/gui/session.c | A desktop bug = an OS bug; can't swap the DE | Desktop becomes a normal replaceable program | Move the WM/compositor out of the kernel into a ring-3 display-server process. |
| 24 | OPEN | P2 | Apps draw into one shared screen buffer | kernel/gui/desktop.c, kernel/gui/fb.c | Garbage from one app shows on the whole screen | Per-window surfaces blended by a compositor | Per-window surfaces (shared-mem canvases) blended by the compositor. |
| 25 | OPEN | P2 | No IPC between apps and desktop | (new) kernel/arch/x86_64/syscall.c (pipe/socket) | Apps can't talk to each other | Real inter-app communication | pipe()/socket() syscalls so processes can talk. |
| 26 | OPEN | P2 | Theming is just a few colors | kernel/gui/theme.h, kernel/gui/app_settings.c | Looks basic and dated | Full themes, dark/light, fonts | Full theming: dark/light, font rendering with antialiasing, animations. |
| 27 | OPEN | P2 | No image viewer / file associations | (new) - needs the app model | Media opens as text / hardcoded dispatch | Real file-type dispatch | An image viewer + a file-type->app dispatch table (needs the app model). |
| 28 | OPEN | P2 | Photo/folder icons look wrong | scripts/gen_assets.py, kernel/gui/icons.c | Users can't tell what a file is | Clear, distinct icons | Redesigned distinct icons (image/folder/video/music). |
| 29 | OPEN | P2 | Icon changes don't propagate (no registry) | kernel/gui/icons.c, kernel/gui/desktop.c | 5 places decide icons separately | One central icon source | A central icon registry so grid/sidebar/dock/desktop agree. |
| 30 | OPEN | P2 | Dock = one icon per window; no grouping/overflow | kernel/gui/desktop.c (layout_dock) | 4 terminals - 4 icons; dock overflows | One icon per app with popup | Dock grouping (one icon per app + popup), overflow paging. |
| 31 | OPEN | P2 | No Alt-Tab / window switcher | kernel/gui/desktop.c | Keyboard users can't switch windows | Alt-Tab overlay | An Alt-Tab / window-switcher overlay. |
| 32 | OPEN | P2 | Wi-Fi/BT/airplane toggles do nothing | kernel/gui/app_settings.c, kernel/gui/session.c | Fake switches mislead | Honest 'not available' | Honest 'not available' states (or real Wi-Fi/BT when drivers exist). |
| 33 | OPEN | P2 | Power menu can't shut down or reboot | kernel/arch/x86_64/acpi.c (no S5) | 'Shut down' does nothing | Real shutdown + reboot | ACPI S5 shutdown + reboot via keyboard controller. |
| 34 | OPEN | P2 | Login is cosmetic; auto-logs-in 'demo' | kernel/gui/session.c, kernel/gui/login_overlay.c | No real user boundary | Real login | A real login screen with account selection (no auto-demo). |
| 35 | OPEN | P2 | 'Sign out' leaves windows lying around | kernel/gui/session.c, kernel/gui/desktop.c | Logging out doesn't clean up | Clean session end | Kill/hide windows on logout; per-session state. |
| 36 | OPEN | P2 | Status bar has no tooltips/volume/network UI | kernel/gui/desktop.c (draw_statusbar) | Info invisible | Tooltips + indicators | Status-bar tooltips + volume/network indicators. |
| 37 | OPEN | P2 | Text rendering is 8x16 bitmap, no antialiasing | kernel/gui/fb.c, kernel/gui/font_data.c | Text looks blocky | Smooth text | A proper font renderer with antialiasing (TTF or raster + smoothing). |
| 38 | OPEN | P2 | Full-screen redraw every frame (no dirty rects) | kernel/gui/desktop.c, kernel/gui/fb.c | Repaints everything | Only changed areas repaint | Dirty-rectangle rendering + vsync (only repaint changed areas). |
| 39 | OPEN | P3 | Windows have no IDs/owners | kernel/include/yart/gui.h, kernel/gui/desktop.c | Can't tell whose window it is | Every window belongs to a process | Window IDs + owner pids; per-window damage; ghost-window cleanup. |
| 40 | OPEN | P3 | Input routed by geometry, not window-ID | kernel/gui/desktop.c (window_at) | Click routing is guesswork | Input goes to the owner | Input routed by window-ID, not geometry guesswork. |
| 41 | OPEN | P3 | No window-manager policies (grouping, thumbnails) | kernel/gui/desktop.c | Taskbar is primitive | Modern taskbar | Grouping, thumbnails, minimize-to-taskbar flyouts. |
| 42 | DONE 10/10 | P4 | All hard-coded apps deleted from the kernel | removed: kernel/gui/app_*.c, apps_catalog.c, apps.h | Kernel is a pure shell; nothing to improve | Perfect as intended | None - perfect as intended (apps deleted; kernel is a pure shell). |
| 43 | OPEN | P4 | No real userland libc | userland/sys.h (78 lines) | Apps can't do much | A standard C library | A real libc (malloc from brk/mmap, printf, errno, stdio). |
| 44 | OPEN | P4 | No exec() / argv / envp | kernel/arch/x86_64/user.c, kernel/fs/elf.c | Can't launch a program by name | Launch programs like real OSes | exec() with argv/envp; dynamic linking; ELF relocations. |
| 45 | OPEN | P4 | No pipes/sockets | kernel/arch/x86_64/syscall.c | No shell pipelines, no network apps | Real shell + network apps | pipe()/socket()/select()/poll() so shells and network apps work. |
| 46 | OPEN | P4 | No media players | comes with the app model | Nothing to open media with | Players | Media players (arrive with the ring-3 app model). |
| 47 | OPEN | P1 | Signals: SIGTERM default-kill works; no handlers/sigreturn yet | kernel/sched/sched.c (sched_signal + delivery), kernel/arch/x86_64/syscall.c (SYS_SIGACTION/RAISE) | SIGTERM kills a busy-looping child (verified status 143); SIGKILL works; raise() + sigaction() exist | Not 10: no user handler trampoline/sigreturn, no blocked-set semantics beyond a bitmask, no SIGCHLD | Signal handlers (sigreturn trampoline); blocked-set semantics; SIGCHLD; per-signal masks. |
| 48 | OPEN | P1 | User-pointer validation + copy_user_str (SMAP-safe) | kernel/arch/x86_64/syscall.c, kernel/mm/vmm.c | All syscall user buffers validated + SMAP-safe copies | Done for existing syscalls; new syscalls must keep the discipline | Extend validation+SMAP discipline to every future syscall (kept). |
---

**Already done (verified in QEMU):** full per-CPU SMP scheduler (4 + 8 cores: per-CPU
current/queues/park slot, per-CPU GDT/TSS/IST + RSP0, preemption on every core, fork
load-balancing + idle work stealing, cross-CPU SIGKILL, safe reap; work stealing
observed, 3 clean consecutive boots); preemptive scheduler + fork(CoW) + waitpid; demand
paging + guard pages + CoW machinery + per-process page tables + RAM swap; page refcounts,
double-free detection, zero-on-free; user-pointer validation; hardened ELF loader; VFS
refcounting; NMI/MCE + spurious IRQ + IDT audit; ring-3 fault isolation; keyboard arrows +
mouse wheel; per-task RSP0 + IST1/2 + swapgs + per-CPU GS; LAPIC/IOAPIC + APIC timer;
virtio-blk disk + persistent YartFS; permissions/owners + doas; private PML4 per task;
FPU/SSE context; NX; ASLR; heap canaries + double-free detection; SIGKILL (kill syscall);
user-fault isolation; userland dynamic memory (mmap/munmap + brk); SHA-256 + salted passwords; signal delivery (SIGTERM default-kill verified, status 143); multi-record journal (div-by-zero / illegal instruction kill only the task, kernel survives).
